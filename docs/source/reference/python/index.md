# Python API

Reference documentation for the `tychopy` VectorFunction Python API.

The worked examples in the rendered docstrings are executed by the
documentation doctest builder (`sphinx-build -b doctest`, the
`tycho_docs_doctest` target), so they act as a regression guard against API
drift.

```{toctree}
:maxdepth: 2

vector_functions
optimal_control
```
